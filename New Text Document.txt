exports.handler = async (event, context) => {
  const data = JSON.parse(event.body);

  console.log("?? New contact form submission:", data);

  return {
    statusCode: 200,
    body: JSON.stringify({ message: "ÅÌ«„ ‘„« »« „Ê›ﬁÌ  «—”«· ‘œ ?" })
  };
};
